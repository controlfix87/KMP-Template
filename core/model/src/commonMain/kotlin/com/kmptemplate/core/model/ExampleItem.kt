package com.kmptemplate.core.model

/**
 * Placeholder domain model. Delete this once real models exist -- it only
 * exists so :feature:example and its test have something concrete to move
 * through the domain/data/presentation layers.
 */
data class ExampleItem(
    val id: String,
    val title: String,
)
